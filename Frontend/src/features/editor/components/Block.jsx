// The active editor renders blocks in its page module. Keep this compatibility
// export inert until the planned feature-level extraction is implemented.
const Block = () => null;

export default Block;
